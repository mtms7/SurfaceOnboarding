# CO-0696 Phase 1 Shadow-Test Record

Date: 2026-08-17  
Mode: shadow validation only — no Salesforce, Workato schedule, Slack, tenant, or Surface configuration write was performed.

## Purpose

CO-0696 is a test clone used to prove the deterministic Phase 1 mismatch path before any future writeback workflow is considered.

## Evidence compared

| Field | Salesforce / DealHub | Contract evidence | Result |
| --- | --- | --- | --- |
| Product | Core Plus Enterprise | Core Plus Enterprise | Matched |
| Start date | 2026-07-31 | 2026-07-31 | Matched |
| End date | 2027-07-30 | 2027-07-30 | Matched |
| License scope | 4,000 End Points | 10,000 End Points | Mismatch |
| Quantity | 4,000 | 10,000 | Mismatch |

Additional gates observed:

- The onboarding approval state remains pending.
- The contract extraction found the explicit Credential Exposure bundle clause, but local OCR requires visual confirmation before treating that evidence as final.

## Deterministic outcome

The local Phase 1 validator returned:

```text
decision: manual_review_required
proceed: false
action_required: contact_csm
mismatches: license_type, quantity
```

This is the expected safe result. The workflow does not select a winning value and does not authorize provisioning.

## Rejection-note preparation

`phase1_validator/rejection_notes.py` was added and unit-tested. It renders a bounded, sanitized note suitable for the future Salesforce `Onboarding Rejection Notes` field. It contains only the CO identifier, mismatch fields/values, gates, policy version, and required action; it excludes raw contract text, file names, contacts, and other document content.

The VM test command completed successfully:

```bash
/home/workato/phase1-venv/bin/python -m unittest \
  phase1_validator.test_server \
  phase1_validator.test_commercial_evidence \
  phase1_validator.test_rejection_notes
```

Result: `Ran 7 tests ... OK`.

## Deliberately not done

- No value was written to CO-0696's `Onboarding Rejection Notes` field.
- No customer or test-clone data was persisted in a Workato recipe body.
- The scheduled Workato recipe remains stopped.
- No browser automation or tenant configuration was invoked.

## Next authorized decision

Before building a Salesforce writeback test, decide whether the future field behavior is **append a timestamped Phase 1 note** (recommended, preserves history) or **replace the field value**. Then explicitly authorize a write to the CO-0696 test clone only. The writeback recipe must read the current field first, update only `Onboarding_Rejection_Notes__c`, and read it back to verify the exact saved note.
